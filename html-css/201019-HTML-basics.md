# HTML 기초

## HTML 역사

현재 웹 표준은 총 3가지

- HTML4 (HTML4.01)
- XHTML ( XHTML 1.0 )
- HTML5

HTML4의 문법은 상당히 느슨

ex) `<H1> head1 <h1>` - o
, `<p> text` - o

=> XML 형식 나옴

XML형식은 문법이 더 엄격 + 좀더 커스텀한 태그 가능

=> W3C에서 XML의 특성을 가미한 XHTML 만듦

- XHTML 2.0 버전 작업을 진행 중이었음

애플, 모질라, 오페라 (벤더 회사) 들은 XHTML의 하위 호환에 치명적 문제가 있음을 알고

WHATWG( ~ ~ Application ~ ~ ~) 공동 설립함 -> W3C와 별개로 Web Application 1.0과 Web Forms2.0 만듦

==> W3C가 공개적으로 XHTML 실패 인정 후 WHATWG의 표준안을 대부분 수용해 HTML5가 탄생

HTML4 에는 API(자바스크립트 영역)가 없었는데 HTML5는 API를 가지고 있어, markup + API 의 특성을 가짐

# 프론트엔드 개발자가 HTML과 CSS까지 모두 해야하는 이유 : 프론트 단에 돌아가는 거 다 통제할 수 있어야 한다.

아웃라인 알고리즘
HTML5 문법 아는 것도 중요하다 무시하지 마라

HTML5 에 왜 새로운 태그들을 추가했나? 아웃라인 알고리즘( 정보구조를 명확히 할 수 있도록 하는 것 ) -> 웹페이지의
정보 구조를 판별할 수 있도록 하는 개념

원래 암묵적인 표기방식으로 구조를 만듦.
ex)

<h1>
	<p></p>
</h1>
<h1>
	<p></p>
</h1>
-> h1블록 2개 이런식으로..

근데 이제 명시적으로 표현하기 위해 article, section 같은 태그들을 만듦
=> headingsMap + web developer 크롬 확장자 통해서 확인 가능
ex)

<article>
	<h1>커피</h1>
		<p></p>
	<article>
		<h1>역사</h1>
			<p></p>
	</article>
</article>
=> 커피의 역사 로 명시적으로 구조 표현 가능

====================================

W3C 애플리케이션

웹 스토리지.. 등

알아보자

====================================

혼자 미니프로젝트 개발할 때도, 체계적으로 해라

-> 어떤 표준, 어떤 기준을 사용해서 개발했다 라고 명시하고 그 기준,표준을 바탕으로 개발
=> 체계적으로 개발했다라는 것을 보여줄 수 있다

======================================

DTD dcoument type dictation
이거 왜 적어주냐?
브라우저한테 문서 타입을 알려주려고

왜 알려주냐? ;; 모르겟
-> 브라우저에게 최신 웹 표준을 사용해서 만든 문서다. 그러니까 최신 웹표준사용해서 구조화해라 라는 것을
알랴줌

====================================

### HTML 구조

- 1. 3단
- 2. 4단

### 논리적 구조

아이디 비밀번호 로그인 로그인상태유지 - x
아이디 로그인 비밀번호 로그인상태유지 - x
아이디 비밀번호 로그인상태유지 로그인 - o

### 시맨틱마크업

```html
<h1>
    <a><image src="" alt=""></a>
</h1>

```

=> h1 시맨틱 요소, a 기능적 요소, image 시각적 요소

### 네이밍

CSS 방법론

1. SMCSS
2. OOCSS
3. BEM -> 최근 많이 사용. TIL/html-css/BEM 참고!

4. pascal case
5. kebab case
6. camel case

======================================

### WAI-ARIA 기술

RIA기술 : 웹 표준이 마련되기 전 third-party가 만든 확장 기술
-> 웹 표준이 아님!

WAI-ARIA => RIA 기술을 Accessible하게 만든 기술

ex)

```HTML
<div role='banner'></div>
<div role='main'></div>
<div role='contentinfo'></div>

<a role="button"></a>
```

- 아무 의미 없는 div태그에 banner,main 등의 시맨틱 의미 부여
- 링크 태그인 a 태그에 button의 시맨틱 역할 부여

=> WAI-ARIA 스택

근데, 굳이 이렇게 안하고 HTML5에 새로 만들어진 태그들을 쓸 수 있다. -> 최근 UI 트렌드

### DIV 태그를 사용하는 경우

```HTML
<header></header>
<div>some ads</div>
<main></main>
<footer></footer>
```

header나 main, footer에 포함되지 않는 요소들을 div태그를 이용해 배치

### id보다 class 속성을 활용하는 이유

id는 재사용이 불가능

최근에는 스타일링을 컴포넌트화 -> 선택자들을 조합하여 완성 시킨다. -> class가 가장 적합

### 스타일링을 컴포넌트화 하는 이유

공통적인 스타일링과 개별로 차이가 나야하는 스타일링을 구분해서 할 수 있기 때문

### class 속성 vs nth-child()

1. class 속성

```CSS
.group{
    display: flex;
}
.group1{
    background: tomato;
}
.group2{
    background: yellowgreen;
}

```

성능 면에서 nth-child보다 선택자를 이용하는 것이 뛰어나다. nth-child()는 DOM트리를 이용해서 찾아가기 때문

2. nth-child()

```CSS
div:nth-child(2n){
    font-weight: bolder;
}
```

요소들을 묶어서 스타일링할 때 더 효율적일 경우가 있다.

### SEO (검색 최적화)

`<title></title>`

title 태그 내부 내용은 **검색 최적화** 와 **웹 접근성** 을 위해 페이지마다 본문 내용의 중요 내용을 넣어, **독립적** 으로 작성해야 한다.

### WCAG

WAI에서 만든 웹 접근성을 위한 가이드라인이다. 최근 버전은 2.1

본문 내용을 입력하기 전에, skip navigation을 두어 웹 접근성을 높이기를 권장하고 있음.

### Web Font

클라이언트가 문서를 요청하면, 클라이언트에 해당 폰트가 설치되어 있지 않다면, 다운받아서 사용하고, 설치되어 있다면 그것을 바로 사용함

한글은 용량이 커서, 큰 용량의 폰트를 사용하면 성능 이슈가 발생할 수 있음 (폰트 다운받아지기 전에 깜빡거림)

추천 폰트 - Noto Sans, **Spoqa Han Sans**

> # 스포카 한 산스 페이지
>
> 용량 다이어트를 감행해 웹 페이지에서도 부담 없이 사용할 수 있습니다.
> 노토 산스를 포함한 한글꼴은 전체 용량이 크기 때문에 웹에서 사용하기에 좋지 않습니다. 이에 11,172자에서 한글꼴 완성형의 최소 단위인 2,350자로 줄였습니다. 첫 버전의 용량은 16MB에서 441KB 수준으로 줄었습니다.
